# OWASP ZAP baseline overrides for the static-export mirror in CI.
# Format: ruleId<TAB>ACTION[<TAB>comment]  — see https://www.zaproxy.org/docs/docker/baseline-scan/
#
# Registry: https://github.com/zaproxy/zaproxy/blob/main/docs/scanners.md

# 50003 — Stats passive scan rule (informational: % 2xx/4xx, content types, endpoint counts — metrics, not vulns)
50003	IGNORE

# 10116 — “ZAP is out of date” (tooling noise in CI images)
10116	IGNORE

# 10109 — “Modern Web Application” (informational)
10109	IGNORE

# 100000 — httpsender script “Client/Server HTTP Error response codes” (4xx/5xx). CI spider still hits occasional misses; nginx + try_files reduces noise.
100000	IGNORE
