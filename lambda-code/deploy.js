// deploy.js lives in the project ROOT, not in lambda-code.
// Run from the parent folder:
console.error("Run deploy from the project root:");
console.error("  cd ..");
console.error("  node deploy.js");
process.exit(1);
